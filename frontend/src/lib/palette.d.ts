/**
 * Type declarations for palette.js — the .js file is consumed by both
 * the Tailwind config (build-time, JS context) and lib/design.ts (TS).
 * Keep this in sync with palette.js shape.
 */
export const colors: {
  readonly bgTier0: string;
  readonly bgTier1: string;
  readonly bgTier2: string;
  readonly fgPrimary: string;
  readonly fgSecondary: string;
  readonly fgTertiary: string;
  readonly borderHairline: string;
  readonly borderStrong: string;
  readonly bullish: string;
  readonly bearish: string;
  readonly accentAmber: string;
  readonly accentCyan: string;
};
